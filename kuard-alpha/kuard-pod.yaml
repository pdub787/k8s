apiVersion: v1
kind: Pod
metadata:
  name: kuard
spec:
  containers:
    - image: pdub787/kuard:blue
      name: kuard
      ports:
        - containerPort: 8080
          name: http
          protocol: TCP
